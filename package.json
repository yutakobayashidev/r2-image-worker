{
  "name": "r2-image-worker",
  "version": "2.0.0",
  "scripts": {
    "build": "moon build --target js --release",
    "dev": "npm run build && wrangler dev ./src/index.ts",
    "deploy": "npm run build && wrangler deploy --minify ./src/index.ts",
    "test": "npm run build && vitest run",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.1",
    "vitest": "^3.2.4",
    "wrangler": "^4.28.1"
  }
}
